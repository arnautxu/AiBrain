# Prioritized AiBrain backlog

Last versioned refresh: 2026-08-30. This is a bounded next-work list, not an idea inventory.

1. **P0 — Current candidate live truth.** Promote only after authorization; correlate Backend CI, Publish, Deploy, digests, runtime and live SHA, then prove restart, two-user isolation and one real turn per user.
2. **P0 — One real governed effect.** Supply one reviewed Arnall connector or browser/document action, bind the real employee credential and approval, execute exactly once and retain provider/controlling-store readback. No generic health or mock satisfies this.
3. **P0 — Recovery proof.** Verify backup, exercise rollback to the recorded previous release, read back identity/data, and re-promote the unchanged candidate.
4. **P1 — Arnall operating inputs.** Confirm team/roles/departments, first internal process, current work/objectives, company preferences, brand pack, tool inventory and GraphikAI support route. Add dated sources; do not replace edited context files.
5. **P1 — Automation operating decision.** Confirm whether Arnall automation execution is enabled for the candidate, name the first bounded automation and prove ownership, audience, idempotency, lease recovery and readback.
6. **P2 — Fleet repeatability.** Reuse the same immutable images and contracts in a second isolated synthetic/customer installation before widening rollout; keep config, secrets, context, data and release state per installation.

Do not add lower-priority surfaces until the P0 evidence gaps are closed or explicitly deferred by the product owner.
