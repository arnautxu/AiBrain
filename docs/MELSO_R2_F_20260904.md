# R2 F — integrated candidate and joined acceptance fixture

Local candidate only. The containing commit is the candidate (`git rev-parse
HEAD`); branch `codex/arnall-melso-f-20260904`, checkout
`/Users/davidliria/Documents/AiBrain-melso-f-20260904`. R1 remains in history:
`bd898707ed590900a90bdd682ea3b39e97d32d25`. Initial remote fetch confirmed
`048a7d4cef7752fe716f66e84e5c52f7ec663022`. No main merge, push, CI, publication,
deployment or customer access. No new tasks/agents/goals/automations.

## Integration and evidence boundaries

Cherry-pick -x, no conflicts:

- A1 `82872898341a97a20205bc2749248db977bc8f8a` → `03f61e7`.
- B `9430e466e85268999cd6b43a6b16189226fd1f43` → `a828344`.
- C `41fc23a4f5ebb78db164b16ce942ccfb86d6a03f` → `9f12faa`.

Preserved Arnau's UI, company/project/chat hierarchy, user-private runtime,
automatic memory and spreadsheet persistence. No duplicated journal or daemon.
Workers' R2 notes carry exact read-only Melso source mappings at
`7c667dd1a41fee4bc2b5172649527cf9f4d26771`. David authorized adaptation; these
are TypeScript adaptations, not copied Go. Melso also uses Codex App Server,
per task; AiBrain keeps its existing persistent per-user service. Names do not
prove equivalent behavior or speed.

| Adopted mechanism | Data-backed result | Unknown / independent gate |
| --- | --- | --- |
| Generation-fenced startup, drain-before-release ownership | Deterministic R1 delayed-spawn and stop/init regressions now pass locally | Independent races, Linux process ownership; old EPERM cause |
| Complete lexical-prefix publication and authoritative final replacement | Mock first publication 24→8 ms in one sample; final no longer concatenated | No distribution/provider speed claim; partial-secret publication and legacy-phase priority QA |
| Atomic projection batch validation | 64-delta fixture: 66→3 full-message validations | Independent invalid-batch atomicity and collision QA |
| Terminal snapshot convergence | complete/stopped/error snapshot stops reattachment in tests | Real interruption/reconnect and no repost QA |
| DOMContentLoaded readiness | C real Chrome151/macOS n=3/page medians: linked 1296→62 ms, form 4318→53, blank 4192→45; navigation-only; capture validated separately | No production/model latency claim; navigation race QA |
| Scoped attachment control, insertText and editing-key mapping | F joined rendered app→signed/authenticated routes→real CDP→DOM readback: exact Unicode, Backspace, click and scroll | Successor/late-release/reconnect stress, OS clipboard/IME and independent verification |
| Bounded linear frame decoder and cancellation | C byte-assembly diagnostic 57.83→0.53 ms (three synthetic 1 MiB frames) | Not FPS; independent abort/error handling QA |

A1's synthetic cold/warm/concurrent before/after ranges overlap. Correctness,
not a speedup, is demonstrated. See A1's note for n=5 generation samples and
nested n=15 chat samples; do not sum medians or present synthetic TTFT as live.

F found two build integration issues: B's new persistence mock inferred
`Promise<undefined>` instead of `Promise<void>`; and the existing exported
`runtimeThreadIdForChatMessage` helper violated Next's route-export contract.
The helper moved unchanged into `src/runtime/chat-thread-selection.ts`, and its
test import moved with it. No assertions or timeout thresholds were weakened.

## Executed gates

- Complete locked dependencies remain F-owned real `node_modules`, no aliases,
  no product package/lock changes. Node24.4.1 / npm11.4.2 / Next16.3.2.
- Union of runtime workers/transports/service, B's 11 files, C's focused browser
  contracts/service/route/frame/input/component files, spreadsheet preview and
  persistence: **29 files passed, 2 opt-in skipped; 248 tests passed, 2 skipped**,
  44.99 s. `/tmp/aibrain-r2-f-tests.log`. One worker, no file parallelism,
  cache=false. No full-suite loop.
- Build `NEXT_TELEMETRY_DISABLED=1 npm run build -- --webpack`: first compiled
  then failed type validation on the two issues above; one corrected rerun
  passed compilation, types, static generation and tracing.
  `/tmp/aibrain-r2-f-build.log`, `/tmp/aibrain-r2-f-build-fixed.log`.
- Final focused run: **5 files / 20 tests PASS**, 3.48 s: isolated real anonymous
  App Server, owned-process, start-stop-race, chat route, HTTP contract.
  `/tmp/aibrain-r2-f-final-tests.log`.
- Isolated pinned Codex `0.149.1` initialize/account-read/cleanup PASS, account
  null, no turn/model request. n=1 cumulative gateway 32.50 ms, initialize
  482.17 ms, account-read 800.65 ms. This does not explain A1's earlier EPERM
  under 0.152.1. R1 ACK5s/ENOTEMPTY remains a separate unresolved flake.
- Typecheck `npx tsc --noEmit --incremental false` and lint of changed TS files
  pass; logs `/tmp/aibrain-r2-f-{typecheck,lint-all}.log`.
- `npm run contracts:verify` PASS. `npm run infra:validate` static boundaries
  PASS; Docker Compose execution unavailable. Final precommit fetch still
  confirms `origin/main=048a7d4cef7752fe716f66e84e5c52f7ec663022`.

## Reproduce the joined rendered-app / gateway / CDP fixture

Harnesses are opt-in scripts, never imported by product routes. No auth bypass
was added to production. `qa-joined-browser.ts` provisions two synthetic local
users, boots the real owner policy, issues real opaque durable local sessions,
and stores Playwright state privately. This **does not test remote Supabase
password login**. Real cookie validation, enabled-user lookup, same-origin,
thread ownership, gateway signatures and control capabilities remain active.

The process runs the compiled app through Next's custom-server API over local
HTTPS3196 with an ephemeral self-signed certificate (client test context uses
ignoreHTTPSErrors). A deny-only proxy on loopback3197 refuses every network
request. An injected fixture factory seeds an about:blank DOM through its owned
private CDP pipe and retains the production Chrome runtime/registry/gateway.
No external website or mock browser endpoint is used. This tests composition,
not the default production factory's Linux sandbox. `CODEX_BIN=/usr/bin/false`
intentionally blocks inference; the visible model-unavailable banner and
worker initialize/send_error logs are expected, not a provider-speed result.

```sh
AIBRAIN_JOINED_QA=1 \
AIBRAIN_CHROME_BIN=/Users/davidliria/Library/Caches/ms-playwright/chromium_headless_shell-1234/chrome-headless-shell-mac-arm64/chrome-headless-shell \
AIBRAIN_CHROME_EXPECTED_VERSION=151.0.7922.34 NODE_ENV=production NEXT_TELEMETRY_DISABLED=1 \
node --import ./scripts/register-server-only-stub.mjs --import tsx scripts/qa-joined-browser.ts
```

It prints only origin, private temporary root, synthetic IDs and readiness.
Use that exact root, not a previous run's session:

```sh
node --import tsx scripts/qa-joined-smoke.ts /absolute/aibrain-joined-qa-ROOT
node --import ./scripts/register-server-only-stub.mjs --import tsx scripts/qa-joined-documents.ts /absolute/aibrain-joined-qa-ROOT
```

Stop with SIGTERM to the printed command's owned process. Browser registry,
HTTPS server, deny proxy and Next close; do not kill processes by a broad name.
Keep storage state private (it contains local test cookies); never publish it.
Fresh run uses fresh roots. F left no server on3196/3197.

Final joined smoke PASS: `/tmp/aibrain-r2-f-smoke.log`; root
`/var/folders/59/6_7xn8_14hg7vs8dqf1rcs040000gn/T/aibrain-joined-qa-KbXkl6`.
Readback `readback-a12f306a-5631-4533-a919-b1042399c673.json`. Final readback:
exact `aCatalà ñ 日本語 👩🏽‍💻`, clicks1, scrollY350; other user's thread read404.
The smoke waits for the appended z before Backspace to avoid a transient false
positive. Paste is a DOM ClipboardEvent, not an OS clipboard claim. Screenshots
are local real-CDP captures; final assertions use DOM readback, not OCR.
Initial setup probes exposed missing HTTPS/secret/owner configuration; only the
harness was corrected. No application authentication check was disabled.

## Documents and provider blockers

The document helper generates synthetic PDF/XLSX and uses real authenticated
upload/preview routes. The executed PDF upload was rejected with
`DOCUMENT_STORAGE_BACKPRESSURE` before preview. F's filesystem readback:
available10,158,751,744 bytes; required13,329,101,620 bytes (5% floor plus two
512MiB upload reservations). Do not lower this guard to manufacture a pass.
Log `/tmp/aibrain-r2-f-documents.log`; fixture `documents.json` records429.
Run on a filesystem with sufficient safety headroom, or obtain authorization
for targeted recoverable cleanup; unrelated user data was not removed.
Signed-in Library PDF/XLSX UI is **not passed**. C's real service-level four-format
generation/conversion evidence remains distinct. QA can use the private local
sessions without the R1 demo-login/composer prerequisite, but must verify the
actual Library/preview surface after storage admission succeeds.
LibreOffice and Poppler are present at the macOS defaults. Neither
`/usr/local/bin/aibrain-qpdf` (production default) nor `/opt/homebrew/bin/qpdf`
exists here. Production-mode preview additionally needs a real QPDF executable
specified by `AIBRAIN_QPDF_BIN`; do not substitute a no-op sanitizer. C's
service fixture selected requireQpdf according to availability, which is weaker
than this production composition. That distinction was not visible from its
summary alone and must remain explicit.

Pinned executable prepared without changing product dependencies:
`/tmp/aibrain-r2-f-codex-01491/node_modules/.bin/codex` (`codex-cli 0.149.1`).
Install used npm prefix/cache outside product. Versioned project configs and QA records
contain synthetic/example installations, not a verified own-account binding.
Only `.env.example` exists in F's checkout. This is not a claim that no eligible
own-account environment exists elsewhere; none was identified/authorized here.
The desktop auth file exists (metadata only inspected); its presence is not
authorization to copy/use it. No desktop/Melso/client credential was read/copied.
`/etc/aibrain` is absent locally. Real provider turns require an operator-named
isolated InstallationConfig, provisioned test-user UUID, and an explicitly
authorized own-account credential bound to that user (or reviewed shared-qa
auth.json inside that installation's dataRoot, private regular file). Verify
account/read against the intended account before a few benign cold/warm/three-
chat/tool prompts. Do not run them against this offline browser fixture.

Docker CLI remains absent; Linux/container and deployment acceptance are open.
Quota at handoff phase check remains45% used; no reset or new account purchased.
Independent QA should prioritize partial-output redaction, invalid atomic batch,
final/legacy selection, terminal no-repost, owner succession and readiness races.
F's first green is not final acceptance; reuse the existing two QA tasks.
